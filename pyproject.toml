[tool.poetry]
name = "shifts-processor"
version = "0.1.0"
description = "A shift scheduling solution using OR-Tools."
authors = ["Your Name <your.email@example.com>"]

[tool.poetry.dependencies]
python = ">=3.11,<4.0"
ortools = "*"
flask = "^3.1.0"
gunicorn = "^23.0.0"

[tool.poetry.scripts]
run-service = "shift_scheduler_service:app"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"